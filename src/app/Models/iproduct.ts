export interface IProduct {
  id: number;
  name: string;
  title: string;
  quantity: number;
  description: string;
  price: number;
  image: string;
  categoryId: number;
  purchaseDate: any;
  isPurchased: boolean;
}
